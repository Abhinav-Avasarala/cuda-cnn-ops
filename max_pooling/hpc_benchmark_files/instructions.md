## See README

This file is kept for compatibility. The authoritative HPC benchmarking guide is:

- `max_pooling/hpc_benchmark_files/README.md`

It includes the full CUDA + OpenMP + single-thread workflow (LSF, Slurm, and manual runs).
